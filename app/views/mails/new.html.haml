%h1 New mail

= form_for @mail, url: mails_path do |f|
  - if @mail.errors.any?
    %div{ id: 'error_explanation' }
      %h2
        = "#{ pluralize(@mail.errors.count, 'error') }"

      %ul
      - @mail.errors.full_messages.each do |msg|
        %li= msg

  = render partial: 'form', locals:{f: f}

= link_to 'Back', root_path